pkgname=vim-anywhere
pkgver=0.0.1
pkgrel=1
arch=("x86_64" "i686")
source=('git+https://github.com/cknadler/vim-anywhere')

build() {
	:;
}

package() {
	mkdir -p "$pkgdir"/opt/vim-anywhere
	cp -r "$srcdir/vim-anywhere" "$pkgdir"/opt
}

